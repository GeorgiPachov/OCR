package com.fmitextrecognition.io;

public interface Bitmap {
	int getPixel(int x, int y);
	int[][] cloneMatrix();
	int[][] asMatrix();
	int getWidth();
	int getHeight();
	Bitmap rescale(int x, int y);
	Bitmap subBitmap(int x1, int y1, int x2, int y2);
}
